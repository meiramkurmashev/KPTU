

<h1>Корректировка/удаление записи</h1>
<form method="get" action="/Directory/ConsumersChangeSave">
    <br />

    @foreach (var wag in ViewBag.a)
    {
        <table>
            <tr><td><b><label>Мин-во</label></b></td><td><input  name=id1 value=@wag.Id1></td></tr>
            <tr><td><b><label>Респ.</label></b></td><td><input  name=id2 value=@wag.Id2></td></tr>
            <tr><td><b><label>Порядк.</label></b></td><td><input readonly name=id3 value=@wag.Id3></td></tr>

            <tr>
                <td><label>Наименование</label></td>
                <td>
                    <input name=name value="@wag.Nm">
                </td>
            </tr>
            <tr>
                <td><label>English name</label></td>
                <td>
                    <input name=name2 value="@wag.Nme">
                </td>
            </tr>
            
            <tr><td><b><label>Ж/д станция</label></b></td><td>
    <b>
         <select id="siF7" name="id4" onchange="update(event.target, 'bdF18')">
            <option value="@wag.Id4" selected>@wag.Nm_st</option>
            @foreach (var wag1 in ViewBag.c)
            {


                <option value="@wag1.Id">@wag1.Id - @wag1.Name</option>


            }
        </select>
</td></tr>
            <tr><td><b><label>Группа</label></b></td><td>
    <b>
        <select id="siF7" name="id5" onchange="update(event.target, 'bdF18')">
            <option value="@wag.Id5" selected>@wag.Nm_gr</option>
            @foreach (var wag1 in ViewBag.b)
            {


                <option value="@wag1.Id">@wag1.Id - @wag1.Name</option>


            }
        </select>
</td></tr>
            <tr><td><b><label>Код потребителя 4</label></b></td><td><input name=id6 value=@wag.Id6></td></tr>
            <tr><td><b><label>Адрес потребителя</label></b></td><td><input name=adres value=@wag.Adres></td></tr>

        </table>
    }
    <br />
    <button type="submit" value="" onclick="location.href='@Url.Action("ConsumersChangeSave")'">Сохранить</button>

    <button type="button" onclick="javascript:history.go(-1);">Назад</button>
    <br /> <br />

    <script>


               function myFunc() {
                   if (confirm("Вы уверены?") == true) {
                       CallFailureOK();
                   }
                   else {
                       CallFailureCancel();
                   }

               }
               function CallFailureCancel() {


               }
               //Html.ActionLink("Удалить", "CoalGradesDelete", "Directory", new { id = ViewBag.id }, null),onclick = "myFunc()" }
               function CallFailureOK() {
                   /**/
                   location.href = "/Directory/ConsumersDelete?id1="+@ViewBag.id;
               /**/


           }
    </script>
    <button onclick="myFunc()" type="button">Удалить</button>
</form>
<br />

